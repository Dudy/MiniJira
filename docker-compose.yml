services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: minijira
      POSTGRES_USER: minijira
      POSTGRES_PASSWORD: minijira
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
